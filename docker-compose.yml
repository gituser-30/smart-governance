version: '3.8'
services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
  
  mongodb:
    image: mongo:latest
    ports:
      - "27017:27017"

# Backend, Frontend, and AI Service are typically expected to be run via their own dev scripts for local dev, or built here for prod.
